name: Build Project

on:
  workflow_dispatch: {}
  pull_request:
    types: [opened, synchronize]

permissions:
  checks: write
  contents: read
  pull-requests: write

jobs:
  build:
    name: Build ${{ matrix.targetPlatform }}
    runs-on: [self-hosted]
    strategy:
      fail-fast: false
      matrix:
        targetPlatform:
          - WebGL
          # - StandaloneWindows64
          # - StandaloneOSX
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Build
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          RUNNER_SUDO_PASSWORD: ${{ secrets.RUNNER_SUDO_PASSWORD }}
        run: |
          # 1. Create license file from secret (safely via env var)
          rm -rf ./Unity_v6000.x.ulf
          echo "$UNITY_LICENSE" > ./Unity_v6000.x.ulf

          # 2. Run Unity Build in Docker
          # Using the image that successfully pulled earlier
          IMAGE="unityci/editor:ubuntu-6000.3.5f1-webgl-3"

          # Ensure build directory exists and has permissions
          mkdir -p build/WebGL
          mkdir -p Library
          chmod -R 777 build
          chmod -R 777 Library

          echo "Memory before build:"
          free -h

          echo "$RUNNER_SUDO_PASSWORD" | sudo -S docker run --rm --privileged \
            -v "$(pwd):/project" \
            -v "$(pwd)/Unity_v6000.x.ulf:/project/Unity_v6000.x.ulf" \
            -w /project \
            -e UNITY_LICENSE_FILE=/project/Unity_v6000.x.ulf \
            $IMAGE \
            unity-editor \
            -verbose \
            -batchmode \
            -nographics \
            -noaudio \
            -quit \
            -manualLicenseFile /project/Unity_v6000.x.ulf \
            -projectPath . \
            -executeMethod Builder.BuildWebGL \
            -logFile build.log

          echo "Buid finished. Exit code: $?"
          echo "Dumping logs:"
          cat build.log || echo "Log file not found"

      - name: Fix Permissions
        if: always()
        env:
          RUNNER_SUDO_PASSWORD: ${{ secrets.RUNNER_SUDO_PASSWORD }}
        run: echo "$RUNNER_SUDO_PASSWORD" | sudo -S chown -R $USER:$USER build

      - uses: actions/upload-artifact@v4
        with:
          name: Build-${{ matrix.targetPlatform }}
          path: build/${{ matrix.targetPlatform }}
